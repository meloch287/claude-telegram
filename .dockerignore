node_modules
dist
.git
*.log

# Данные и секреты. Всё это монтируется томами при запуске, поэтому в образе
# не нужно — а попав туда, остаётся в слоях навсегда.
data
workspaces
.env
.env.*
!.env.example

# Домашний каталог агента: в нём .claude.json с токеном подписки и транскрипты
# всех сессий. Монтируется в /home/claude, копировать его в образ незачем.
claude-home

# Конфигурация прокси с учётными данными VLESS.
proxy
