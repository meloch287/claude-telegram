# Состояние

Обновляется по ходу работы. Дата последней правки — 14 августа 2026.

## Работает

| Что | Где |
|---|---|
| Чат с агентом, живая сессия на чат | `src/agent/conversation.ts` |
| Разрешения карточками с кнопками | `src/agent/permissions.ts` |
| Вход по подписке или API-ключу | `src/auth.ts` |
| `/resume` — прошлые чаты кнопками | `src/index.ts` |
| Смена модели и режима разрешений | `src/bot/keyboards.ts` |
| Проекты — отдельная папка на проект | `src/bot/session.ts` |
| Учёт расхода токенов (дельтами) | `src/agent/conversation.ts` |
| Мини-апп: коты, достижения, статистика | `src/miniapp/` |
| Развёрнуто на 203.0.113.10 через прокси | `docker-compose.yml` |
| Кнопки «Назад», роутер экранов | `src/bot/screens.ts` |
| Выбор канала выхода пробой к API | `src/proxy.ts` |
| Сбор лимитов подписки в базу | `src/agent/conversation.ts`, `src/db.ts` |
| Предупреждение в чат при подходе к лимиту | `src/bot/session.ts` |
| `/status` — канал и лимиты | `src/index.ts` |

## Делается сейчас

- Экран лимитов в мини-аппе (сбор данных готов, вёрстка на ревью доступности)

## Следующее по порядку

1. **Германский пул прокси.** Сейчас выход идёт через Tor — не та страна, и
   Tor-выходы блокируют. Код пула готов, нужны сами адреса. См. [02](02-сеть-и-прокси.md).
2. **Экран лимитов в мини-аппе**: полосы по окнам с отметкой возраста данных.
3. **Периодическая перепроверка канала** и переключение на живой прокси.
4. **Голосовые сообщения** — после выбора способа распознавания.
5. **Пересланные сообщения и файлы**.

## Что проверено на практике

- `claude setup-token` существует и требует подписку — вывод `--help`.
- `ApiKeySource` включает `'oauth'`; CLI знает `CLAUDE_CODE_OAUTH_TOKEN` — типы
  SDK и строки в бинаре.
- `SDKRateLimitEvent` отдаёт `five_hour` и `seven_day` с `utilization` и
  `resetsAt` — типы SDK.
- С 203.0.113.10 напрямую до Anthropic **403** (страна RU), через
  `egress-proxy:1080` — **401**. Проба различает случаи.
- Существующий `egress-proxy` ходит через **Tor**, а не через Германию: xray
  шлёт в порт 19050, Cloudflare отдаёт `loc=T1`, выходной адрес меняется между
  пробами.
- Нативный бинарь `claude` приезжает опциональной зависимостью и в образе есть.
- Мини-апп снаружи отдаёт 200, `/api/profile` без подписи — 401.

## Что НЕ проверено

- Приходит ли `rate_limit_event` по API-ключу или только по подписке.
- Как часто он приходит.
- Работает ли `resume` после перезапуска контейнера (сессии лежат в томе, но
  сквозной проверки не было).
- Держит ли `egress-proxy` длинные стриминговые ответы — короткие пробы прошли,
  реальная сессия не гонялась.

## Долги

- `src/tiers.ts` остался от выпиленной системы тарифов. Используется только ради
  типа `TierId` в базе. Убрать вместе со столбцом `tier`.
- В `docker-compose.yml` сеть `telegram-archive_egress` зашита именем чужого
  проекта. Для переносимости вынести в переменную.
- `AUTH_MODE` и `OWNER_ANTHROPIC_API_KEY` в конфиге больше не используются.
