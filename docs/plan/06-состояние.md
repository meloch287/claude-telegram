# Состояние

Обновляется по ходу работы. Дата последней правки — 15 августа 2026.

## Работает

| Что | Где |
|---|---|
| Чат с агентом, живая сессия на чат | `src/agent/conversation.ts` |
| Разрешения карточками с кнопками | `src/agent/permissions.ts` |
| Вход по подписке или API-ключу | `src/auth.ts` |
| `/resume` — прошлые чаты кнопками | `src/index.ts` |
| Смена модели и режима разрешений | `src/bot/keyboards.ts` |
| Проекты — отдельная папка на проект | `src/bot/session.ts` |
| Учёт расхода токенов (дельтами) | `src/agent/conversation.ts` |
| Мини-апп: коты, достижения, статистика | `src/miniapp/` |
| Развёрнуто на 203.0.113.10 через прокси | `docker-compose.yml` |
| Кнопки «Назад», роутер экранов | `src/bot/screens.ts` |
| Выбор канала выхода пробой к API | `src/proxy.ts` |
| Сбор лимитов подписки в базу | `src/agent/conversation.ts`, `src/db.ts` |
| Предупреждение в чат при подходе к лимиту | `src/bot/session.ts` |
| `/status` — канал и лимиты | `src/index.ts` |
| Свой германский прокси-контейнер | `docker-compose.yml`, `scripts/make-proxy-config.py` |
| Экран лимитов в мини-аппе | `src/miniapp/public/app.js` |
| Название чата и сообщение о его начале | `src/index.ts` |
| Импорт истории из транскриптов | `scripts/import-history.ts` |
| `/clone` — забрать репозиторий в проект | `src/bot/repos.ts` |
| Доступ к GitHub: git по токену и `gh` в образе | `src/agent/conversation.ts`, `Dockerfile` |
| Живая отрисовка ответа по мере генерации | `src/bot/output.ts` |
| Мини-апп на кнопке меню Telegram | `src/index.ts` |
| Голосовые: расшифровка через Polza и показ текста | `src/bot/voice.ts` |
| Пересланное с подписью автора, серия одним куском | `src/index.ts` |
| Созданные файлы возвращаются в чат | `src/bot/artifacts.ts` |
| `/file` — забрать файл из проекта | `src/index.ts` |
| Длинный код уходит файлом, дифф сжат до сводки | `src/agent/render.ts` |
| Субагенты видны в ленте активности | `src/agent/conversation.ts` |
| Скиллы, CLAUDE.md и правила проекта | `src/agent/conversation.ts` |
| MCP-серверы из файла конфигурации | `src/mcp.ts` |
| `/compact` и `/clear` | `src/index.ts` |
| План приходит карточкой с кнопкой «принят» | `src/bot/keyboards.ts` |
| Ход мысли виден, пока агент думает | `src/agent/conversation.ts` |
| Вывод команд и ошибки инструментов в чате | `src/agent/conversation.ts` |
| Отчёт о сжатии контекста, `/context` с полосой | `src/agent/conversation.ts`, `src/index.ts` |
| Фоновые задачи отчитываются о завершении | `src/agent/conversation.ts` |
| Обрыв связи с API виден в ленте | `src/agent/conversation.ts` |
| Видео, кружки, гифки, стикеры, гео, контакты, опросы | `src/index.ts` |
| Ответ на непонятный тип вместо молчания | `src/index.ts` |
| В группе бот отзывается только на обращение | `src/index.ts` |
| `/resume все` — чаты по всем проектам | `src/index.ts` |
| Картинки возвращаются фотографией | `src/bot/output.ts` |
| MCP: context7 и deepwiki, чтение без подтверждений | `mcp.json`, `src/mcp.ts` |
| Личные скиллы пользователя — 56 штук | `claude-home/.claude/skills` |
| Ежедневная копия базы, хранение неделю | `src/backup.ts` |
| Healthcheck контейнера через `/healthz` | `docker-compose.yml`, `src/miniapp/server.ts` |
| Перепроверка канала на ходу и переключение | `src/proxy.ts` |
| `/diff`, `/commit`, `/push`, `/pr` — git из чата | `src/bot/git.ts` |
| CI: типы и тесты на каждый пуш | `.github/workflows/ci.yml` |
| Деплой на сервер после зелёного main | `scripts/deploy.sh` |

## Следующее по порядку

1. **Третий-четвёртый германский канал.** Сейчас работают два; три сервера из
   подписки используют шифрование, которое эта сборка xray не умеет.
2. **Перезапуск нездорового контейнера.** Healthcheck состояние показывает, но
   сам не перезапускает: для этого нужен сторож с доступом к `docker.sock`, а это
   root-эквивалент на хосте, где живут чужие проекты. Решение за владельцем.
3. **Напоминания по расписанию не делаем.** `Cron` и `PushNotification` агенту
   доступны, но сторож для них не пишется: пользователь их не хочет.

## Что проверено на практике

- `claude setup-token` существует и требует подписку — вывод `--help`.
- `ApiKeySource` включает `'oauth'`; CLI знает `CLAUDE_CODE_OAUTH_TOKEN` — типы
  SDK и строки в бинаре.
- `SDKRateLimitEvent` отдаёт `five_hour` и `seven_day` с `utilization` и
  `resetsAt` — типы SDK.
- С 203.0.113.10 напрямую до Anthropic **403** (страна RU), через
  `egress-proxy:1080` — **401**. Проба различает случаи.
- Существующий `egress-proxy` соседнего проекта ходит через **Tor**: xray шлёт
  в порт 19050, Cloudflare отдаёт `loc=T1`, выходной адрес меняется между пробами.
- Свой контейнер `claude-proxy` даёт два рабочих германских канала: проба
  отдаёт 401 и `loc=DE`. Бот на старте выбирает первый живой.
- Нативный бинарь `claude` приезжает опциональной зависимостью и в образе есть.
- Мини-апп снаружи отдаёт 200, `/api/profile` без подписи — 401.
- `cli.github.com` с сервера открывается **напрямую**, в отличие от
  `api.anthropic.com` и `github.com`; прокси нужен второму и третьему.
- С токеном `gh` видит приватные репозитории, приватный клон проходит.
- CLI принимает `--setting-sources` и `--mcp-config`: проба с заведомо негодным
  ключом доходит до `init`, жалоб на неизвестный флаг нет. `--mcp-config`
  подставляется только при непустом наборе серверов, поэтому пустой конфиг
  ничего не ломает.
- В контейнере поднимаются 15 встроенных скиллов, 44 слэш-команды и 5 типов
  субагентов. Инструментов у агента 29, включая `Skill`, `Task`, `Workflow`,
  `Cron*` и `PushNotification`.
- 44 теста проходят внутри работающего контейнера, не только на машине разработки.
- **MCP подключаются на самом деле.** Оба сервера отвечают на рукопожатие
  `initialize` с сервера, а живой `init` SDK показывает
  `[{"name":"context7","status":"connected"},{"name":"deepwiki","status":"connected"}]`
  и пять инструментов `mcp__*`. Удалённый MCP GitHub отказал нашему токену
  (`gho_`, не та область) — его не подключаем, `gh` покрывает то же самое.
- **Скиллов видно 71:** 15 встроенных и 56 личных.
- **Голосовые расшифровываются на самом деле.** `api.polza.ai` открывается с
  сервера напрямую (HTTP 200, прокси не нужен), `/v1/audio/transcriptions`
  принимает multipart. Сквозная проверка модулем `voice.ts` внутри контейнера:
  ogg/opus 11 932 байта → «Привет, это проверка расшифровки голосовых сообщений
  в Боте» за 1,5 секунды. Поле `model` необязательно, но задано явно.
- Реальная история: 3464 файла сессий, 207 900 ответов модели, 63 млн входных
  и 169,6 млн выходных токенов. Чтение кэша — 59,8 **млрд**, то есть в 257 раз
  больше настоящих токенов; поэтому в счёт оно не идёт.

## Ошибки, которые стоили работоспособности

**Секреты попадали в образ.** `.dockerignore` не исключал `claude-home`, и в
каждый слой образа копировался `.claude.json` с токеном подписки вместе со
всеми транскриптами сессий. Каталог монтируется томом в `/home/claude`, то есть
в образе не нужен вовсе. Исключены также `proxy` с учётными данными VLESS.

**Скиллы приехали ссылками в пустоту.** В `~/.claude/skills` 55 из 57 записей —
символические ссылки на `~/.agents/skills`. `rsync -a` копирует сами ссылки, и
внутри контейнера они указывали в никуда: грузилось 2 скилла из 57. Нужен
`rsync -aL`, который разыменовывает. Проверять надо не число каталогов, а число
найденных `SKILL.md`.

**Сессии не переживали пересборку.** Claude Code хранит их в `~/.claude` внутри
контейнера, а тома там не было. Каждая пересборка стирала историю, `/resume` не
находил прошлых чатов, а сохранённый в базе идентификатор указывал в пустоту —
продолжение падало с «No conversation found». Теперь смонтирован весь домашний каталог, а если сессия всё же потерялась, диалог начинается заново и
реплика повторяется сама.

**Контейнер работал от root.** Claude Code отказывается включать режим «без
вопросов» под root: «--dangerously-skip-permissions cannot be used with
root/sudo privileges for security reasons». Теперь в образе заводится
пользователь `claude` (uid 10001), тома принадлежат ему, и режим доступен.

**Дом монтировался наполовину.** Сессии лежат в `~/.claude`, но настройки — в
`~/.claude.json` рядом. Монтируя одну лишь папку, файл теряли при каждой
пересборке, и Claude Code ругался, что конфига нет. Монтируется весь домашний
каталог.

**Порядок распознавания секретов.** Шаблон ключа API `^sk-ant-[A-Za-z0-9_-]{20,}$`
шире и целиком накрывает токен подписки `sk-ant-oat01-…`. Проверялся он первым,
поэтому токен подписки сохранялся как ключ и уходил в `ANTHROPIC_API_KEY`.
Anthropic отвечал «API key is invalid», а бот молчал. Теперь подписочный шаблон
проверяется первым, ключ явно исключает `oat`, а старые записи чинятся при
чтении. Покрыто тестом.

- Столбца `tier` в боевой базе не было: снимок `VACUUM INTO` показал схему
  `users` без него, миграция дропа оказалась холостой. Проверено на копии, а не
  на живой базе.
- `github.com` с сервера открывается напрямую (HTTP 200), проксировать выкатку
  не нужно. Ssh до `git@github.com` тоже доходит — ключ только на чтение.

## Что НЕ проверено

- **`getContextUsage()` на живой подписке.** Проба с подставным ключом упирается
  в 403 раньше ответа. Падение обработано: `/context` отдаёт последнее значение,
  которое SDK кладёт на сообщения агента, и молчит вместо ошибки.
- Приходит ли `rate_limit_event` по API-ключу или только по подписке.
- Как часто он приходит.
- Работает ли `resume` после перезапуска контейнера: том добавлен, но сквозной
  проверки на реальной сессии ещё не было.
- Держит ли `egress-proxy` длинные стриминговые ответы — короткие пробы прошли,
  реальная сессия не гонялась.

## Осознанные ограничения

- **Массовые изменения не отдаются файлами.** Клон репозитория или установка
  зависимостей меняют сотни файлов; выше сорока бот присылает одну строку с
  числом, а не выборку. Иначе результат работы тонул бы в чужих файлах.
- **Голос уходит на сторонний сервис.** Адрес задаёт владелец установки, зашитого
  бэкенда нет: голос — чувствительные данные, и выбор должен быть явным. Сейчас
  это Polza, и запрос идёт напрямую, минуя германский канал: прокси нужен только
  Anthropic, а лишний крюк для российского сервиса — риск без выгоды.

## Долги

Закрыты 15 августа 2026:

- `src/tiers.ts` удалён. Столбца `tier` в боевой базе не оказалось вовсе: таблицу
  создали до тарифов, а `CREATE TABLE IF NOT EXISTS` в существующую таблицу поля
  не добавляет. Мини-апп всё это время читал несуществующее поле и показывал
  «План Free» кому угодно; теперь показывает выбранную модель.
- `AUTH_MODE` и `OWNER_ANTHROPIC_API_KEY` убраны из конфига и `.env.example`.
- Сеть `telegram-archive_egress` из compose ушла ещё раньше: у проекта своя сеть
  `claude-telegram` и свой контейнер прокси.

Остаётся:

- Сервер выкатывается по ssh от `root`. Отдельный пользователь с правами только
  на `/opt/claude-telegram` и docker был бы аккуратнее.
